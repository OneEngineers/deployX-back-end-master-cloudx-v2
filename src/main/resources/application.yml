spring:
  datasource:
    url: jdbc:postgresql://db-svc:5432/myapp
    username: postgres
    password: password@123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect